using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class Enemy6Collision : EnemyCollision
{
    //[SerializeField] float wallCheckDistance;
    //[SerializeField] LayerMask layerMask;
    //[SerializeField] bool isWallDetected;

    //private void Update()
    //{
    //    isWallDetected = Physics2D.Raycast(transform.position, Vector2.right, wallCheckDistance, layerMask);
    //    if (isWallDetected) Debug.Log("ok");
    //}

    //private void OnDrawGizmos()
    //{
    //    Gizmos.DrawLine(transform.position, new Vector3(transform.position.x + wallCheckDistance, transform.position.y));
    //}

}
